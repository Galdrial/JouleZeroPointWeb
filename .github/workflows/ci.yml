name: CI — Joule Zero Point (Optimized)

on:
  workflow_dispatch: # Permette l'avvio manuale dalla scheda Actions di GitHub

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ─────────────────────────────────────────
  # FRONTEND: TypeScript + Unit Tests + Build
  # ─────────────────────────────────────────
  frontend:
    name: Frontend CI
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v5

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install dependencies
        run: npm ci --legacy-peer-deps

      - name: 🔍 TypeScript type check
        run: npx vue-tsc -b --noEmit

      - name: 🧹 ESLint
        run: npm run lint

      - name: 🧪 Run unit tests
        run: npm run test

      - name: 🏗️ Build frontend
        run: npm run build

  # ─────────────────────────────────────────
  # BACKEND: Install + Tests
  # ─────────────────────────────────────────
  backend:
    name: Backend CI
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v5

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧪 Run backend unit tests
        run: npm test
        env:
          NODE_ENV: test

  # ─────────────────────────────────────────
  # DOCKER BUILD: Verifica che le immagini si costruiscano
  # ─────────────────────────────────────────
  docker:
    name: Docker Build Check
    runs-on: ubuntu-latest
    needs: [frontend, backend]

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v5

      - name: 🐳 Build backend image
        run: docker build -t joule-backend:ci ./backend

      - name: 🐳 Build frontend image
        run: docker build -t joule-frontend:ci ./frontend

  # ─────────────────────────────────────────
  # E2E TESTS: Solo PR o manuale
  # ─────────────────────────────────────────
  e2e:
    name: Playwright E2E Tests
    runs-on: ubuntu-latest
    needs: [frontend, backend]

    if: |
      github.event_name == 'pull_request' ||
      github.event_name == 'workflow_dispatch'

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v5

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install dependencies
        run: npm ci --legacy-peer-deps

      - name: 💾 Cache Playwright Browsers
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('frontend/package-lock.json') }}

      - name: 🎭 Install Playwright Browsers
        run: npx playwright install --with-deps chromium

      - name: 🤖 Run E2E tests
        run: npx playwright test
